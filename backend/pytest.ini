[pytest]
testpaths = tests
addopts = -ra -q --strict-markers \
          --cov=activity_api_v2 --cov=activity_api --cov=get_suggestion \
          --cov=log_activity --cov=get_activitieslogperuserid --cov=user_auth \
          --cov-report=term-missing --cov-report=xml:coverage.xml --cov-fail-under=85
markers =
    unit: fast, isolated tests
